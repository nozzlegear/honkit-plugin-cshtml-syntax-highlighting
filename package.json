{
  "name": "@nozzlegear/honkit-plugin-cshtml-syntax-highlighting",
  "version": "0.0.1",
  "main": "dist/index.js",
  "description": "Adds syntax highlighting to cshtml code blocks.",
  "license": "MIT",
  "scripts": {
    "validate": "tsc -p . --skipLibCheck --noEmit",
    "build": "yarn validate && tsc -p ."
  },
  "engines": {
    "honkit": ">1.x.x"
  },
  "honkit": {
    "properties": {}
  },
  "devDependencies": {
    "honkit": "^4.0.4",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "highlightjs-cshtml-razor": "^2.1.1"
  }
}
